name: mirrorchyan

on:
  workflow_dispatch:
    inputs:
      tag:
        default: ""
        required: false

jobs:
  mirrorchyan_mxu:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        os: [win, linux]
        arch: [aarch64, x86_64]
        exclude:
          - os: linux
            arch: aarch64
    steps:
      - uses: MirrorChyan/uploading-action@v1
        with:
          filetype: latest-release
          filename: ${{ format('MaaYYs-{0}-{1}-*-MXU*', matrix.os, matrix.arch) }}
          mirrorchyan_rid: MaaYYs
          tag: ${{ inputs.tag }}

          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}

          owner: TanyaShue
          repo: MaaYYs
          github_token: ${{ secrets.GITHUB_TOKEN }}
          upload_token: ${{ secrets.MirrorChyanUploadToken }}

  mirrorchyan_exec:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        os: [macos]
        arch: [aarch64, x86_64]
    steps:
      - uses: MirrorChyan/uploading-action@v1
        with:
          filetype: latest-release
          filename: ${{ format('MaaYYs-{0}-{1}-*-MXU.dmg', matrix.os, matrix.arch) }}
          mirrorchyan_rid: MaaYYs_exec
          tag: ${{ inputs.tag }}

          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}

          owner: TanyaShue
          repo: MaaYYs
          github_token: ${{ secrets.GITHUB_TOKEN }}
          upload_token: ${{ secrets.MirrorChyanUploadTokenMac }}
