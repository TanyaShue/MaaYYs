name: mirrorchyan

on:
  workflow_dispatch:
    inputs:
      tag:
        default: ""
        required: false

jobs:
  mirrorchyan_mxu:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: win
            arch: x86_64
            extension: zip
          - os: win
            arch: aarch64
            extension: zip
          - os: macos
            arch: x86_64
            extension: dmg
          - os: macos
            arch: aarch64
            extension: dmg
          - os: linux
            arch: x86_64
            extension: tar.gz
    steps:
      - uses: MirrorChyan/uploading-action@v1
        with:
          filetype: latest-release
          filename: ${{ format('MaaYYs-{0}-{1}-*-MXU.{2}', matrix.os, matrix.arch, matrix.extension) }}
          mirrorchyan_rid: MaaYYs
          tag: ${{ inputs.tag }}

          os: ${{ matrix.os }}
          arch: ${{ matrix.arch }}

          owner: TanyaShue
          repo: MaaYYs
          github_token: ${{ secrets.GITHUB_TOKEN }}
          upload_token: ${{ secrets.MirrorChyanUploadToken }}
